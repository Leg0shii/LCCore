package de.legoshi.lccore.command.flow;

import team.unnamed.inject.AbstractModule;

public class LinkcraftCommandModule extends AbstractModule {
    @Override
    protected void configure() {
        // TODO: bind warp...
        //bind(WarpCommand.class).singleton();
    }
}
